const COLORS = ['red', 'blue', 'green', 'yellow'];
const MOVE_TIME = 30000;
const SAFE_ZONES = [29,42,55,16,63,24,37,50]; // Starting positions and colored squares

module.exports = { COLORS, MOVE_TIME, SAFE_ZONES };
